{
  "name": "fortitwin-unified",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "cd apps/web && npm run dev",
    "dev:ai": "cd apps/ai-engine && python -m uvicorn app.main:app --reload --port 8000",
    "install:all": "npm install && cd apps/web && npm install"
  }
}